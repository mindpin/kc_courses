= simple_form_for [@course, @chapter], wrapper: :kc_courses do |f|
  .form-inputs
    = f.input :title
    = f.input :desc, as: :text
  
  .form-actions
    = f.button :submit

%a{href: (course_path(@chapter.course))} 返回
