= form_for [@chapter, @ware] do |f|
  .field
    = f.text_field :title, placeholder: '课件标题'
  .field
    = f.text_area :desc, placeholder: '课件描述'

  = f.submit '提交'

%a{href: (chapter_path(@ware.chapter))} 返回

